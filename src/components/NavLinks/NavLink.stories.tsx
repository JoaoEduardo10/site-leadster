import { Meta } from '@storybook/react';
import { NavLink } from '.';

export default {
  title: 'NavLink',
  component: NavLink,
} as Meta;

export const StoryNavLink = () => {
  return <NavLink />;
};
