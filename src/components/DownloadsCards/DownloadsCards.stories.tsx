import { Meta } from '@storybook/react';
import { DownloadsCards } from '.';

export default {
  title: 'DownloadsCards',
  component: DownloadsCards,
} as Meta;

export const StoryDownloadsCards = () => {
  return <DownloadsCards />;
};
