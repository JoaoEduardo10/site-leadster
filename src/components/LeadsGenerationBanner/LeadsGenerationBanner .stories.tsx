import { Meta } from '@storybook/react';
import { LeadsGenerationBanner } from '.';

export default {
  title: 'LeadsGenerationBanner',
  component: LeadsGenerationBanner,
} as Meta;

export const StoryLeadsGenerationBanner = () => {
  return <LeadsGenerationBanner />;
};
